providers = ["python", "node"]

[phases.setup]
nixPkgs = ["python311", "nodejs"]

[phases.build]
cmds = [
    "pip install -r requirements.txt",
    "cd zedin-frontend && npm install && npm run build"
]

