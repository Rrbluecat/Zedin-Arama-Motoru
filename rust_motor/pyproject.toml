[build-system]
requires = ["maturin>=1.5"]
build-backend = "maturin"

[project]
name = "zedin_domain"
version = "3.0.0"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]

